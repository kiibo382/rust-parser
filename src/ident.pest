alpha = { 'a'..'z' | 'A'..'Z' }
digit = { '0'..'9' }
digit_head = { '1'..'9' }

pos_int = { digit_head ~ (digit)+ }
digit_int = { digit+ }

ident = { (alpha | digit)+ }
ident_list = _{ !digit ~ ident ~ (" " ~ ident)+ }
